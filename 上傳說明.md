# React 版本上傳到 GitHub 說明

## 📋 步驟

### 1. 在 GitHub 創建新倉庫

1. 前往 https://github.com/new
2. 倉庫名稱：`shun1010-react`
3. 設為 **Private**（如果需要）
4. **不要**初始化 README、.gitignore 或 license
5. 點擊 "Create repository"

### 2. 上傳代碼

```bash
cd "/Users/caijunchang/Desktop/程式專案資料夾/shun1010-常順班表/shun1010-react"

# 確認 remote 已設置
git remote -v

# 如果沒有，添加 remote：
# git remote add origin https://github.com/sky770825/shun1010-react.git

# 推送代碼
git push -u origin main
```

### 3. 部署到 Cloudflare Pages

#### 方法 A: 使用 GitHub 自動部署（推薦）

1. 前往 Cloudflare Dashboard → Pages
2. 點擊 "Create a project" → "Connect to Git"
3. 選擇 GitHub 並授權
4. 選擇 `shun1010-react` 倉庫
5. 建置設置：
   - **Framework preset**: Vite
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
6. 環境變數：
   - `VITE_SUPABASE_URL` = `https://qrgarecutiwjrgshslli.supabase.co`
   - `VITE_SUPABASE_ANON_KEY` = `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
   - `VITE_USE_SUPABASE` = `true`
7. 點擊 "Save and Deploy"

#### 方法 B: 使用 Wrangler CLI

```bash
npm run build
npx wrangler pages deploy dist --project-name=shun1010-react
```

### 4. 訪問應用

部署完成後，應用將在以下網址可用：
- `https://shun1010-react.pages.dev`
- 或您設定的自訂網域

## ✅ 完成

現在 React 版本已經完全獨立，與舊版本分開部署和維護！
